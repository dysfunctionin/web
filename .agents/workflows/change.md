# Change workflow

1. Start from the concrete file, behavior, or failure.
2. Form one local hypothesis and one cheap falsifying check.
3. Make the smallest consistent edit.
4. Run the narrowest workspace lint/build check immediately.
5. Repair the same slice before broadening.

Follow existing app patterns. Add shared root code only when multiple subdomains actually need it.